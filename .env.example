# Required for `npm run discover` (the LLM-driven discovery loop).
# Not needed for `npm run replay`, `npm run mock-app`, `npm run serve`, or tests.
ANTHROPIC_API_KEY=

# Optional overrides
CLAUDE_MODEL=claude-sonnet-5
MOCK_APP_PORT=4000
HANDOFF_PORT=4100
CAPABILITY_API_PORT=4200
